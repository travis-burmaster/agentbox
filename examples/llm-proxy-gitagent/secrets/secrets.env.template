# AgentBox + LLM Proxy secrets
# Copy this file to secrets.env and fill in your values.
# secrets.env is gitignored.

# Your Claude OAuth token from Claude Max subscription
# Get it from: ~/.claude/.credentials.json → claudeAiOauth.accessToken
# Or: ~/.config/claude/credentials.json on some systems
CLAUDE_OAUTH_TOKEN=your-claude-oauth-token-here

# Telegram bot token (optional) — get from @BotFather on Telegram
# When set, openclaw will accept messages via your Telegram bot
TELEGRAM_TOKEN=
