# AgentBox A2A — Environment Variables
# Copy to .env for local Docker testing. Never commit .env.
#
# On Cloud Run, WORKSPACE_REPO is set in service.yaml (plain env var).
# GITHUB_TOKEN and ENCRYPTION_KEY are injected from Google Secret Manager.
# All other secrets live in secrets_encrypted.enc inside the workspace repo.

# ── Required ──────────────────────────────────────────────────────────────────

# GitHub path to your private workspace repo (owner/repo — no .git suffix)
WORKSPACE_REPO=owner/your-workspace-repo

# Fine-grained GitHub PAT with read+write access to WORKSPACE_REPO
# (On Cloud Run this comes from Secret Manager: agentbox-github-token)
GITHUB_TOKEN=github_pat_xxxx

# Key used to decrypt secrets_encrypted.enc from the workspace repo
# (On Cloud Run this comes from Secret Manager: agentbox-encryption-key)
ENCRYPTION_KEY=your-strong-passphrase-here

# ── Deployment config (substituted into service.yaml via cloudbuild) ──────────
SMTP_FROM=bot@yourdomain.com

# ── Optional overrides (defaults set in Dockerfile) ───────────────────────────
# AGENTBOX_BACKEND=cli
# OPENCLAW_CONFIG_PATH=/agentbox/.openclaw/openclaw.json
# WORKSPACE_SYNC_INTERVAL=60   # seconds between memory sync to GitHub

# ── GCP (Cloud Run only) ──────────────────────────────────────────────────────
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# EXPECTED_AUDIENCE=https://agentbox-a2a-xxxx-uc.a.run.app
# EXPECTED_CALLER_SA=gemini-agent@your-project.iam.gserviceaccount.com
